# ===== Required =====
# GitHub token used for search API calls.
# Fine-grained token: Repository access -> Only public repositories (Read); Permissions -> Issues: Read-only; Metadata: Read-only
# Classic token: no scopes is enough for public search; add "repo" if you want to include private repos.
GITHUB_TOKEN=ghp_your_token_here

# At least one Slack delivery path:
# Preferred: Bot token (chat.postMessage). Make sure the bot is invited to your channel:  /invite @YourAppName
SLACK_BOT_TOKEN=xoxb_your_slack_bot_token_here
# Optional fallback: an Incoming Webhook URL
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# ===== Recommended =====
# Where to post the digest (channel name or channel ID)
SLACK_CHANNEL=$SLACK_CHANNEL

# Your GitHub username (used to learn top languages when USE_PROFILE_LANGS=true)
GITHUB_USERNAME=$GITHUB_USER_NAME

# ===== Search Filters =====
# Use languages from your profile (true/false).
USE_PROFILE_LANGS=true

# Whether to apply a language: filter in GitHub Issue search (can be over-strict; set to false for backfills).
USE_LANGUAGE_FILTER=true

# If not using profile languages, list static languages (GitHub Linguist names)
STATIC_LANGUAGES=TypeScript,Go,Python

# Bounty-ish labels to look for (OR logic)
LABELS=bounty,💎 Bounty,reward,algora

# Optional repo focus (owner/repo, comma-separated). Leave blank to search all public repos.
REPOS=

# When collecting, how far back to consider in minutes (freshness window)
WINDOW_MINUTES=12

# Algora org slugs to watch (comma-separated). Leave blank to skip Algora.
# Examples: tuist,qdrant,triggerdotdev,TheCompaniesAPI,revertinc
ALGORA_ORGS=

# ===== Digest Controls =====
# How far back to include items when sending the digest (minutes)
DIGEST_LOOKBACK_MIN=60
# Minimum items required to send a digest
DIGEST_MIN_COUNT=1
# Safety cap on how many rows to include in one digest
MAX_ITEMS_IN_DIGEST=50
# Character budget for a Slack message (the script uses this to decide whether to thread or split)
MAX_SLACK_CHARS=5000
# Post long details in a thread (true/false). If false and the header would be shortened, details are omitted.
POST_LONG_AS_THREAD=false
# Disable Slack link previews if you prefer compact messages
SLACK_UNFURL=true

# ===== CSV & Storage =====
# Write a CSV alongside every digest
WRITE_CSV=true
# Directory for CSVs
CSV_DIR=/home/$USER/bounties/csv
# Upload the CSV file to Slack along with the message (requires files:write)
UPLOAD_CSV_TO_SLACK=false

# SQLite DB path (auto-created)
BOUNTY_DB=/home/$USER/bounties/bounties.db

# ===== Logging =====
LOG_DIR=/home/$USER/bounties/log
LOG_LEVEL=INFO

# ===== One-time bootstrap (backfill) =====
# Days to backfill when MODE=bootstrap
BOOTSTRAP_DAYS=7
